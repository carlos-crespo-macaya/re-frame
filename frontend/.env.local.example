# Local Development Environment Variables
# Copy this file to .env.local and update values as needed

# Backend API URL
# IMPORTANT: Always use http://localhost:8000 for browser access
# The Docker service name 'backend' only works for server-side rendering
NEXT_PUBLIC_API_URL=http://localhost:8000

# Environment mode
NODE_ENV=development

# Service name (used in health checks)
SERVICE_NAME=re-frame-frontend

# Port (if you need to change from default 3000)
# PORT=3000