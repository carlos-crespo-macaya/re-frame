// Root layout for internationalized app
// The [locale]/layout.tsx handles the actual HTML structure
export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}